# ~/.phasty.yaml — user-level defaults shared across all projects
# Project phasty.config.js and CLI args take precedence over these.

php:
  bin: php
  # bin: /usr/local/bin/php8.3

server:
  host: 0.0.0.0

# https:
#   cert:
#     email: admin@example.com
#     dns:
#       credentialsFile: ~/.secrets/cloudflare.ini
